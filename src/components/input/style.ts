import { StyleSheet } from "react-native";
import { colors } from "@/styles/colors";

export const styles = StyleSheet.create({
    container: {
        height: 52,
        width: "100%",
        backgroundColor: colors.gray.grey11,
        borderRadius: 8,
        borderWidth: 1,
        borderColor: colors.gray.Silver,
        padding: 10,
        color: colors.gray.Silver,
        fontSize: 16
    },
})